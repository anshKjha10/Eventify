

export const authService = {
  login:    async (credentials) => ({ data: {} }),
  register: async (data)        => ({ data: {} }),
  getMe:    async ()            => ({ data: {} }),
  logout:   async ()            => {},
}
